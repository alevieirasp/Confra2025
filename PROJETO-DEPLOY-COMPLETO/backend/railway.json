{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd src && gunicorn main:app --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/api/reservations",
    "healthcheckTimeout": 100
  }
}

